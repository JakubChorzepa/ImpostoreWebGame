{
  "name": "impostor-game",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"yarn dev:server\" \"yarn dev:client\"",
    "dev:server": "yarn --cwd server dev",
    "dev:client": "yarn --cwd client dev",
    "install:all": "yarn --cwd server && yarn --cwd client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
